# Lista de Tarefas

- [x] Criar o arquivo de proposta `openspec/changes/subjects-api/proposal.md`.
- [x] Criar o arquivo de especificação `openspec/changes/subjects-api/specs/subjects-api/spec.md`.
- [x] Criar o endpoint `POST /subjects`.
- [x] Criar o endpoint `GET /subjects`.
- [x] Criar o endpoint `GET /subjects/{id}`.
- [x] Criar o endpoint `PATCH /subjects/{id}`.
- [x] Criar o endpoint `DELETE /subjects/{id}`.
- [x] Criar o script `scripts/find_overdue_subjects.py` que recebe uma lista de atividades em JSON, compara a `due_date` com a data atual e retorna um JSON com uma lista de `subject_id` únicos que possuem tarefas atrasadas.
- [x] Criar uma nova função no Xano (`search_subjects_logic`) que busca disciplinas e atividades, chama o script Python quando `has_overdue_tasks=true`, e filtra os resultados.
- [x] Criar o novo endpoint `GET /subjects/search` que utiliza a função `search_subjects_logic` para retornar os dados filtrados.
- [x] Conectar o formulário de cadastro de disciplina da página `1_📚_Disciplinas.py` ao endpoint `POST /subjects`.
- [x] Melhorar a exibição da lista de disciplinas em `1_📚_Disciplinas.py`, selecionando e renomeando as colunas mais importantes.
- [x] Adicionar um botão de "Excluir" para cada disciplina na lista em `1_📚_Disciplinas.py` que chame o endpoint `DELETE /subjects/{id}`.
- [x] Adicionar um botão de "Editar" para cada disciplina, permitindo a atualização dos dados através de um formulário que chame o endpoint `PATCH /subjects/{id}`.
- [x] Criar o arquivo de especificação `openspec/changes/activity-management/specs/activity-db/spec.md` para a tabela `academic_activities`.
- [x] Criar a definição da tabela `tables/academic_activities.xs`.
- [x] Criar o arquivo de proposta `openspec/changes/activity-management/proposal.md`.
- [x] Criar o arquivo de especificação `openspec/changes/activity-management/specs/activity-api/spec.md`.
- [x] Criar o endpoint `POST /academic_activities`.
- [x] Criar o endpoint `GET /academic_activities`.
- [x] Criar o endpoint `PATCH /academic_activities/{id}`.
- [x] Criar o endpoint `DELETE /academic_activities/{id}`.
- [x] Criar o arquivo de proposta `openspec/changes/activity-grading/proposal.md`.
- [x] Criar a definição da tabela `tables/activity_grades.xs`.
- [x] Criar o arquivo de especificação `openspec/changes/activity-grading/specs/grade-api/spec.md`.
- [x] Criar o endpoint `POST /activity_grades` para permitir o lançamento de notas.
- [x] Criar o endpoint `GET /activity_grades` para listar as notas.
- [x] Criar o endpoint `PATCH /activity_grades/{id}` para permitir a atualização de notas.
- [x] Criar a página `2_📝_Atividades.py` no frontend para listar e cadastrar atividades.
- [x] Conectar o formulário de cadastro da página `2_📝_Atividades.py` ao endpoint `POST /academic_activities`.
- [x] Criar o arquivo de proposta `openspec/changes/subjects-db/proposal.md`.
- [x] Criar o arquivo de especificação `openspec/changes/subjects-db/specs/subjects-db/spec.md`.
- [x] Criar a definição da tabela `tables/subjects.xs`.
- [x] Criar a página `pages/1_📚_Disciplinas.py` no frontend para listar, cadastrar, editar e excluir disciplinas.
- [x] Criar o arquivo de proposta `openspec/changes/calculate-progress/proposal.md`.
- [x] Criar o arquivo de especificação `openspec/changes/calculate-progress/specs/calculate-progress/spec.md`.
- [x] Garantir que o script `scripts/calculate_progress.py` esteja alinhado com a especificação.